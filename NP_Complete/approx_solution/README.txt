From the home directory 'NP_COMPLETE', change directories into 'NP_COMPLETE/approx_solution' using this command:

cd approx_solution

Then execute the program by running the 'run_example.sh' file using the bash command:

bash run_example.sh